terraform {
  required_providers {
    random = {
      source = "hashicorp/random"
    }
  }
}

resource "random_pet" "cat" {
  count = 0
}

# uncomment this for the 0.13.6 run
resource "random_pet" "platypus" {}
